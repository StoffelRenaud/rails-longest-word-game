<div class="wrapper">
  <h1>Bienvenue !!!!!!!</h1>
  <h2>Des chiffres et des lettres</h2>
</div>

<div class="container">
  <p>Voici les lettres disponibles</p>
  <p>Quel est le mot le plus long</p>
  <% @grid.each do |lettre| %>
      <%= lettre %>
  <% end %>
  <form action="score" method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :grid, @grid %>
    <input type="text" name="word" placeholder="What's your word">
    <input type="submit">
  </form>
</div>

